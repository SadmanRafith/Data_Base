CREATE TABLE STUDENT
(
  S_ID NUMBER(10) CONSTRAINT S_ID_PK PRIMARY KEY,
  S_NAME VARCHAR2(20),
  PHONE NUMBER,
  ADDRESS VARCHAR2(50),
  EMAIL VARCHAR2(30),
  CREDIT_COMPLETED NUMBER(3),
  COURSE_COMPLETED NUMBER(2),
  CGPA FLOAT
);

DESC STUDENT

ALTER TABLE STUDENT ADD(
 PETS NUMBER(5)
);

ALTER TABLE STUDENT ADD(
 DEPARTMENT VARCHAR2(5),
 GENDER VARCHAR2(6)
);



ALTER TABLE STUDENT RENAME COLUMN DEPARTMENT TO DEPT

ALTER TABLE STUDENT MODIFY CGPA NUMBER(4,3) 

ALTER TABLE STUDENT DROP COLUMN EMAIL

RENAME STUDENT TO STUDENTS

DESC STUDENTS

SELECT * FROM STUDENTS











CREATE TABLE DISHA
(
  D_ID NUMBER(10) CONSTRAINT D_ID_PK PRIMARY KEY,
  D_NAME VARCHAR2(20),
  PHONE NUMBER
);

DESC DISHA

INSERT INTO DISHA VALUES (1, 'uwu', 12)

INSERT INTO DISHA VALUES (12, 'T', NULL)

INSERT INTO DISHA VALUES (13, 'D', NULL)

SELECT * FROM DISHA


DELETE FROM DISHA WHERE PHONE = 12

TRUNCATE TABLE DISHA

DELETE DISHA